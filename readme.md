# 🍽️ GoodFood API

REST API для получения случайных рецептов блюд по категориям.

> Пет-проект для изучения бэкенд-разработки на Go.

## 🚀 Технологии

- **Go 1.24** — язык программирования
- **PostgreSQL** — реляционная база данных
- **Docker** — контейнеризация окружения
- **Swagger UI** — интерактивная API-документация
- **pgxpool** — эффективный пул соединений с БД

## 📦 Быстрый старт

### Требования
- Docker и Docker Compose
- Go 1.21+

### 1. Клонировать репозиторий
bash:
git clone https://github.com/ValdrGo/GoodFoodApi.git
cd GoodFoodApi

### 2. Настроить переменные окружения
bash:
# Скопируй шаблон
cp .env.example .env  # или создай вручную
# Отредактируй .env при необходимости

### 3. Запусти базу данных
bash:
docker-compose up -d

### 4. Запусти сервер
bash: go run cmd/api/main.go

### 5. Открыть документацию
http://localhost:8080/swagger/index.html

Тестирование API
Через Swagger UI
Откройте http://localhost:8080/swagger/index.html и используйте кнопку "Try it out" для интерактивного тестирования.
Пример ответа:
{
  "id": 1,
  "title": "Овсяная каша с ягодами",
  "category": "breakfast",
  "description": "Полезный завтрак...",
  "ingredients": [
    {"name": "Овсяные хлопья", "amount": "50г"},
    {"name": "Молоко", "amount": "200мл"}
  ],
  "instructions": "1. Залить хлопья молоком...",
  "kcal": 350,
  "protein": 12,
  "fat": 8,
  "carbs": 55,
  "created_at": "2026-02-24T17:55:49Z"
}

Структура проекта:
GoodFoodApi/
├── cmd/api/
│   └── main.go           # Точка входа, настройка сервера
├── internal/
│   ├── handler/          # Обработка HTTP-запросов
│   ├── service/          # Бизнес-логика и валидация
│   ├── repository/       # Работа с базой данных, SQL-запросы
│   └── model/            # Структуры данных (DTO)
├── docs/                 # Swagger-документация (авто-генерация)
├── docker-compose.yml    # Конфигурация Docker
├── .env.example          # Шаблон переменных окружения
├── go.mod                # Зависимости Go
└── README.md             # Этот файл

Доступные категории:
breakfast 🍳 Завтрак
lunch 🍲 Обед
dinner 🍝 Ужин
snack 🥗 Перекус

Архитектура:
Проект использует слоистую архитектуру для разделения ответственности:
HTTP Request
     ↓
[Handler] → валидация входных данных, HTTP-логика
     ↓
[Service] → бизнес-логика, правила приложения
     ↓
[Repository] → работа с базой данных, SQL-запросы
     ↓
[Model] → структуры данных, JSON-сериализация

Автор: Vladimir Pluzhnikov
Год: 2026
Статус: 🟢 Учебный проект / Портфолио